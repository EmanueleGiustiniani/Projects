package sudProject.items.itemTypes;

import sudProject.items.Item;

public class UniqueItem extends Item {

    // /--CONSTRUCTORS--/
    public UniqueItem(String itemName, int value, boolean dropped, String item_description) {
        super(itemName, value, dropped, item_description);
    }
}
